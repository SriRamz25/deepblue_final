# ──────────────────────────────────────────────────────────
# Sentra Pay Backend — Environment Variables
# Copy this to .env and fill in your values.
# On Render: add these in Dashboard → Environment Variables
# ──────────────────────────────────────────────────────────

# PostgreSQL — Neon (neon.tech)
# Get this from Neon dashboard → Project → Connection Details → Connection string
# Make sure to append ?sslmode=require
DATABASE_URL=postgresql://username:password@ep-xxx-yyy.us-east-2.aws.neon.tech/neondb?sslmode=require

# JWT
SECRET_KEY=change-me-to-a-random-64-char-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Redis (optional — leave blank to use in-memory FakeRedis)
REDIS_URL=

# App
ENVIRONMENT=production
DEBUG=false

# CORS — set to your Netlify URL after deploy, e.g.:
# CORS_ORIGINS=["https://sentra-pay.netlify.app"]
# Use ["*"] during initial testing
CORS_ORIGINS=["*"]

# Cloudinary (for profile avatar uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Google OAuth (optional)
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
